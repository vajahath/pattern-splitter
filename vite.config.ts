import { VitePWA } from 'vite-plugin-pwa';
import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [VitePWA({
    registerType: 'autoUpdate',
    injectRegister: false,

    pwaAssets: {
      disabled: false,
      config: true,
    },

    manifest: {
      name: 'pattern-splitter',
      short_name: 'splitter',
      description: 'This application is a web-based SVG pattern tiling tool that converts a single, oversized vector file into a multi-page, true-to-scale PDF for printing on standard home paper, complete with alignment marks, cut lines, and a scale ruler for accurate assembly',
      theme_color: '#4338ca',
    },

    workbox: {
      globPatterns: ['**/*.{js,css,html,svg,png,ico}'],
      cleanupOutdatedCaches: true,
      clientsClaim: true,
    },

    devOptions: {
      enabled: false,
      navigateFallback: 'index.html',
      suppressWarnings: true,
      type: 'module',
    },
  })],
})